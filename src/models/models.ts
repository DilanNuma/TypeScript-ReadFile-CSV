export interface DataRow{
    [key:string]:string //arreglo de string
}

export type DataTable = DataRow[]

export type ColumName = string[];//arreglo de eestilos para hacer las filas y columnas (tablas)
